#!/usr/bin/env sh
. "$(dirname -- "$0")/common.sh"

# Типы проверяем целиком по проекту (astro check) и только здесь: в pre-commit
# это дало бы ложные ошибки, проверка по отдельным файлам не видит остальной код.
yarn typecheck
