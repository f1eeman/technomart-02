# Адрес API изнутри сети. Читается в рантайме: в сборку не зашивается.
API_BASE=http://localhost:3000

# Порт сайта и адрес схемы для `yarn api:types`.
PORT=4321
API_SCHEMA_URL=http://localhost:3000/api/openapi.json

# Порт хоста для docker compose.
WEB_PORT=4321
