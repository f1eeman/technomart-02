# Сид из catalog.ts

Status: ready-for-agent
PRD: .scratch/backend/PRD.md

38 товаров переезжают из `src/data/catalog.ts` фронта в базу — дословно, вместе с
ценами, характеристиками и шутками про восемь метров: это копирайт исходника.

Две правки при переносе:

- `titleLines` схлопывается в `title` одной строкой. Единственный товар с ручным
  переносом — «Профессиональная селфи-палка» — теперь ломается браузером. Это
  четвёртое сознательное расхождение с оригиналом, его надо дописать в `CLAUDE.md`.
- `image` из метаданных `astro:assets` превращается в путь к файлу.

Четыре картинки исходника кладутся в `uploads` и переиспользуются по кругу, как
это и было.

## Приёмка

- Повторный запуск сида не плодит дубли и не падает.
- В базе 38 товаров, по шести разделам, суммы цен совпадают с `catalog.ts`.

## Comments

**Сделано.** 38 товаров лежат в базе.

Данные перенесены разовой выгрузкой: `src/data/catalog.ts` фронта прогнан
скриптом, который подменил импорты картинок путями и склеил `titleLines` в
`title` через пробел. Результат — `prisma/seed-data.json` в репозитории API;
скрипт выгрузки удалён, потому что повторять её незачем: дальше товары правит
админка, а не файл фронта.

Сид `prisma/seed.ts` проверяет данные схемой zod (в том числе «ровно три пары
характеристик») и льёт их `upsert` по `slug` — отсюда идемпотентность.

Картинки: четыре файла исходника скопированы в `uploads/`, товары
переиспользуют их по кругу — тот же порядок, что считал `imageFor` во фронте.
Express раздаёт `/uploads` статикой; в compose папка смонтирована именованным
томом, чтобы загруженное админкой пережило пересборку образа.

Проверено:

- в базе 38 товаров по шести разделам: моноподы 12, виртуальная реальность 6,
  остальные по 5
- сумма цен 875350 — ровно столько же даёт `catalog.ts`
- второй прогон сида: те же 38 строк, ни дублей, ни падения
- `/uploads/item-1.jpg` — 200, `image/jpeg`, 6469 байт; несуществующий файл — 404
- `yarn lint`, `yarn typecheck`, `prettier --check`, `docker compose up --build` —
  чисто

**Расхождение с оригиналом, о котором предупреждала задача.** Название
«Профессиональная селфи-палка» больше не разбито на две строки руками — в базе
одна строка, перенос отдан браузеру. Это четвёртое сознательное расхождение, оно
уже описано в `CLAUDE.md`.
